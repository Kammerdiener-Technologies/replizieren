name: E2E Tests

on:
  push:
  pull_request:

jobs:
  test-e2e:
    name: E2E (K8s ${{ matrix.k8s-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # Test against multiple Kubernetes versions
        # See https://github.com/kubernetes-sigs/kind/releases for available images
        # Latest patch versions from kind v0.31.0; v1.29 and v1.30 dropped (EOL, not available in kind v0.31.0)
        # v1.35+ drops cgroup v1 support — requires cgroup v2 on the host
        k8s-version:
          - v1.35.0
          - v1.34.3
          - v1.33.7
          - v1.32.11
          - v1.31.14

    steps:
      - name: Clone the code
        uses: actions/checkout@v5

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Install the latest version of kind
        run: |
          curl -Lo ./kind https://kind.sigs.k8s.io/dl/latest/kind-linux-amd64
          chmod +x ./kind
          sudo mv ./kind /usr/local/bin/kind

      - name: Verify kind installation
        run: kind version

      - name: Running Test e2e on Kubernetes ${{ matrix.k8s-version }}
        run: |
          go mod tidy
          make test-e2e KIND_K8S_VERSION=${{ matrix.k8s-version }}

  # Summary job that reports overall success/failure
  test-e2e-summary:
    name: E2E Test Summary
    needs: test-e2e
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Check test results
        run: |
          if [ "${{ needs.test-e2e.result }}" == "success" ]; then
            echo "All E2E tests passed!"
            exit 0
          else
            echo "Some E2E tests failed!"
            exit 1
          fi
